package token

import (
    // "_fmt"
)

func Token() {

    const (
        IDENTIFIER = "IDENTIFIER"
        NUMBER     = "NUMBER"
        PLUS       = "PLUS"
        EQUAL      = "EQUAL"
        EOF        = "EOF"

    )
}
